package ca.frc6390.athena.hardware.encoder;

/**
 * User-facing unit meaning for an encoder ref after its conversion chain.
 */
public enum EncoderUnit {
    RAW,
    ROTATIONS,
    DEGREES,
    RADIANS,
    METERS
}
